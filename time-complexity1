#include<stdio.h>
#include<time.h>
int main(){
    long a=500;
    long c=0;
    clock_t start=clock();
    for(int i=0;i<a;i++)
        c++;
    clock_t end=clock();
    printf("Time taken= %d\n",end-start);
    return 0;
}
